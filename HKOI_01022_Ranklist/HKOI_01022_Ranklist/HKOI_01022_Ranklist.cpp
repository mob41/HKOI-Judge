// HKOI_01022_Ranklist.cpp : Defines the entry point for the console application.
//

#include "stdafx.h"
#include <iostream>
#include <string>
#include <vector>

int main()
{
	int num = 0;

	std::cin >> num;

	char pNchar[10];
	int score = 0;

	std::vector<std::vector<std::string>> vec(2, std::vector<std::string>(num));

	for (int x = 0; x < num; x++) {
		std::cin >> pNchar;
		std::string pN(pNchar);
		std::cin >> score;

		if (intvec.size() > 1) {
			if (score > )
		}
	}
    return 0;
}

